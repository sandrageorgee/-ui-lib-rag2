Component: badge
============================================================

Interface: IBadgeProps

Description:
Interface for the Badge component. A small numeric or status descriptor anchored to a child element.

Dependencies: IBadgeProps

Allows additional props: False

------------------------------------------------------------

Default Values:
  max: 99
  showZero: false
  variant: standard
  color: error
  invisible: false

CSS Classes:
  - ?
  - Cui-Badge${isHidden

Import:
  import { Badge } from '@common-ui';

Demo Examples:

  --- Default ---

  () => {
    return (
      <div style={{ padding: 40, display: 'flex', gap: 32, alignItems: 'center' }}>
        <Badge count={4}>
          <span style={{ fontSize: 28 }}>🔔</span>
        </Badge>
        <Badge count={0} showZero>
          <span style={{ fontSize: 28 }}>📧</span>
        </Badge>
        <Badge count={120} max={99}>
          <span style={{ fontSize: 28 }}>💬</span>
        </Badge>
      </div>
    );
  }


  --- Dot Variant ---

  () => {
    return (
      <div style={{ padding: 40, display: 'flex', gap: 32, alignItems: 'center' }}>
        <Badge variant="dot" color="error">
          <span style={{ fontSize: 28 }}>🔔</span>
        </Badge>
        <Badge variant="dot" color="success">
          <span style={{ fontSize: 28 }}>📧</span>
        </Badge>
        <Badge variant="dot" color="primary">
          <span style={{ fontSize: 28 }}>💬</span>
        </Badge>
      </div>
    );
  }


  --- Colors ---

  () => {
    const colors = ['primary', 'secondary', 'error', 'warning', 'success', 'info'];
    return (
      <div style={{ padding: 40, display: 'flex', gap: 32, flexWrap: 'wrap', alignItems: 'center' }}>
        {colors.map(color => (
          <div key={color} style={{ textAlign: 'center' }}>
            <Badge count={3} color={color}>
              <span style={{ fontSize: 28 }}>★</span>
            </Badge>
            <p style={{ margin: '8px 0 0', fontSize: 12 }}>{color}</p>
          </div>
        ))}
      </div>
    );
  }

----------------------------------------

Storybook Stories:

  --- Default ---

  fontSize: 28
  count: 4

  <Badge
    fontSize={28}
    count={4}
  />


  --- Exceeds Max ---

  count: 120
  max: 99
  fontSize: 28

  <Badge
    count={120}
    max={99}
    fontSize={28}
  />


  --- Dot Variant ---

  variant: dot
  color: error
  fontSize: 28
  count: 4

  <Badge
    variant="dot"
    color="error"
    fontSize={28}
    count={4}
  />


  --- Show Zero ---

  count: 0
  showZero: true
  fontSize: 28

  <Badge
    count={0}
    showZero
    fontSize={28}
  />


  --- Success Color ---

  count: 3
  color: success
  fontSize: 28

  <Badge
    count={3}
    color="success"
    fontSize={28}
  />

============================================================

Component Props:

Interface: IBadgeProps

Component: badge
Interface: IBadgeProps
Prop: children

The 'children' prop belongs to the IBadgeProps interface in the Badge component.
Type: ReactNode
Required: No

Description:
The content the badge is anchored to. Typically an icon or button.

Accepted types:
- React.ReactElement
- string
- number
- Iterable<ReactNode>
- React.ReactPortal
- boolean

Usage:
The 'children' prop is used in the IBadgeProps interface of the Badge component.

----------------------------------------

Component: badge
Interface: IBadgeProps
Prop: count

The 'count' prop belongs to the IBadgeProps interface in the Badge component.
Type: number
Required: No

Description:
The numeric value displayed inside the badge. Ignored when variant is "dot".

Accepted values:
- number

Usage:
The 'count' prop is used in the IBadgeProps interface of the Badge component.

----------------------------------------

Component: badge
Interface: IBadgeProps
Prop: max

The 'max' prop belongs to the IBadgeProps interface in the Badge component.
Type: number
Required: No

Description:
If count exceeds this value, the badge displays `${max}+`.

Accepted values:
- number

Usage:
The 'max' prop is used in the IBadgeProps interface of the Badge component.

----------------------------------------

Component: badge
Interface: IBadgeProps
Prop: showZero

The 'showZero' prop belongs to the IBadgeProps interface in the Badge component.
Type: boolean
Required: No

Description:
Controls whether the badge is visible even when count is zero.

Accepted values:
- boolean

Usage:
The 'showZero' prop is used in the IBadgeProps interface of the Badge component.

----------------------------------------

Component: badge
Interface: IBadgeProps
Prop: variant

The 'variant' prop belongs to the IBadgeProps interface in the Badge component.
Type: BadgeVariant
Required: No

Description:
Visual variant of the badge.
- "standard" renders the count number.
- "dot" renders a small coloured dot with no text.

Enum values:
- standard
- dot

Usage:
The 'variant' prop is used in the IBadgeProps interface of the Badge component.

----------------------------------------

Component: badge
Interface: IBadgeProps
Prop: color

The 'color' prop belongs to the IBadgeProps interface in the Badge component.
Type: BadgeColor
Required: No

Description:
Colour of the badge indicator.

Enum values:
- default
- primary
- secondary
- error
- info
- success
- warning

Usage:
The 'color' prop is used in the IBadgeProps interface of the Badge component.

----------------------------------------

Component: badge
Interface: IBadgeProps
Prop: invisible

The 'invisible' prop belongs to the IBadgeProps interface in the Badge component.
Type: boolean
Required: No

Description:
When true the badge is not rendered.

Accepted values:
- boolean

Usage:
The 'invisible' prop is used in the IBadgeProps interface of the Badge component.

----------------------------------------

Component: badge
Interface: IBadgeProps
Prop: className

The 'className' prop belongs to the IBadgeProps interface in the Badge component.
Type: string
Required: No

Description:
Additional CSS class names applied to the root element.

Accepted values:
- string

Usage:
The 'className' prop is used in the IBadgeProps interface of the Badge component.

----------------------------------------


================================================================================
