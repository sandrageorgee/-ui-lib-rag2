Component: spinner
============================================================

Interface: ISpinnerProps

Allows additional props: False

------------------------------------------------------------

CSS Classes:
  - Cui-Spinner__track

Import:
  import { Spinner } from '@common-ui';

Demo Examples:

  --- Default ---
  () => {
    return (
      <div style={{ padding: 100 }}>
        <Spinner />
      </div>
    );
  }

  --- izes ---
  () => {
    return (
      <div style={{ padding: 40, display: 'flex', alignItems: 'center', gap: 32 }}>
        <div style={{ textAlign: 'center' }}>
          <Spinner size="small" />
          <p style={{ margin: '8px 0 0', fontSize: 12 }}>small</p>
        </div>
        <div style={{ textAlign: 'center' }}>
          <Spinner size="medium" />
          <p style={{ margin: '8px 0 0', fontSize: 12 }}>medium</p>
        </div>
        <div style={{ textAlign: 'center' }}>
          <Spinner size="large" />
          <p style={{ margin: '8px 0 0', fontSize: 12 }}>large</p>
        </div>
      </div>
    );
  }

  --- Colors ---
  () => {
    return (
      <div style={{ padding: 40, display: 'flex', alignItems: 'center', gap: 32 }}>
        <Spinner color="primary" label="Primary" />
        <Spinner color="secondary" label="Secondary" />
        <div style={{ color: '#9c27b0' }}>
          <Spinner color="inherit" label="Inherit" />
        </div>
      </div>
    );
  }

Storybook Stories:

  --- Default ---

  --- Small ---
  Props: size=small

  --- Large ---
  Props: size=large

  --- Secondary ---
  Props: color=secondary, size=medium

  --- Inherit Color ---
  Props: color=inherit

Component Props:

Interface: ISpinnerProps

Component: spinner
Interface: ISpinnerProps
Prop: size

The 'size' prop belongs to the ISpinnerProps interface in the spinner component.
Type: string
Required: No

Description:
Controls the 'size' behavior of the component.

Accepted values:
- small
- medium
- large

Usage:
The 'size' prop is used in the ISpinnerProps interface of the spinner component.

----------------------------------------

Component: spinner
Interface: ISpinnerProps
Prop: color

The 'color' prop belongs to the ISpinnerProps interface in the spinner component.
Type: string
Required: No

Description:
Controls the 'color' behavior of the component.

Accepted values:
- primary
- secondary
- inherit

Usage:
The 'color' prop is used in the ISpinnerProps interface of the spinner component.

----------------------------------------

Component: spinner
Interface: ISpinnerProps
Prop: label

The 'label' prop belongs to the ISpinnerProps interface in the spinner component.
Type: string
Required: No

Description:
Accessible label announced by screen readers.

Accepted values:
- string

Usage:
The 'label' prop is used in the ISpinnerProps interface of the spinner component.

----------------------------------------

Component: spinner
Interface: ISpinnerProps
Prop: className

The 'className' prop belongs to the ISpinnerProps interface in the spinner component.
Type: string
Required: No

Description:
Additional CSS class names applied to the root element.

Accepted values:
- string

Usage:
The 'className' prop is used in the ISpinnerProps interface of the spinner component.

----------------------------------------


================================================================================
