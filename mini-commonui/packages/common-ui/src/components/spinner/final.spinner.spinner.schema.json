{
  "component": "spinner",
  "interface": "ISpinnerProps",
  "originalInterface": "ISpinnerProps",
  "type": "object",
  "properties": {
    "size": {
      "$ref": "#/definitions/SpinnerSize",
      "description": "Size of the spinner wheel.",
      "default": "medium"
    },
    "color": {
      "$ref": "#/definitions/SpinnerColor",
      "description": "Colour variant of the spinner.",
      "default": "primary"
    },
    "label": {
      "type": "string",
      "description": "Accessible label announced by screen readers.",
      "default": "Loading"
    },
    "className": {
      "type": "string",
      "description": "Additional CSS class names applied to the root element."
    }
  },
  "additionalProperties": false,
  "definitions": {
    "SpinnerSize": {
      "type": "string",
      "enum": [
        "small",
        "medium",
        "large"
      ]
    },
    "SpinnerColor": {
      "type": "string",
      "enum": [
        "primary",
        "secondary",
        "inherit"
      ]
    }
  }
}